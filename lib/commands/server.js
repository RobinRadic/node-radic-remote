var radic = require('radic'),
    rr = require('../'),
    util = radic.util,
    path = require('path'),
    manager = require('daemon-manager');
module.exports = function (cli) {

  //  cli.generateCommand('server', )
};